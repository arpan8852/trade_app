import 'package:flutter/material.dart';

class GttScreen extends StatelessWidget {
  const GttScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("gtt screen"),
    );
  }
}